% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mega_forecasting.R
\name{simulate_strat}
\alias{simulate_strat}
\title{Simulate Stratified Voting Outcomes}
\usage{
simulate_strat(
  vote_prediction,
  vote_sd,
  turnout_prediction,
  turnout_sd,
  n = 10,
  conf_level = 0.999
)
}
\arguments{
\item{vote_prediction}{A numeric vector of predicted voting intentions.}

\item{vote_sd}{A numeric vector of standard deviations for vote predictions.}

\item{turnout_prediction}{A numeric vector of predicted turnout likelihoods.}

\item{turnout_sd}{A numeric vector of standard deviations for turnout predictions.}

\item{n}{An integer specifying the number of simulations per prediction set. Default is 10.}

\item{conf_level}{A numeric value representing the confidence level. Default is 0.999.}
}
\value{
A data frame with columns 'set_id', 'dem', 'rep', and 'not_voted', representing
        the count of Democrat, Republican, and non-voted individuals for each set of inputs.
}
\description{
This function simulates voting outcomes based on vote and turnout predictions.
It generates random samples for each set of inputs, and computes the number of
'rep', 'dem', and 'not_voted' votes.
}
\examples{
# Example usage:
vote_prediction <- c(0.6, 0.4)
vote_sd <- c(0.1, 0.1)
turnout_prediction <- c(0.7, 0.5)
turnout_sd <- c(0.1, 0.1)
simulate_strat(vote_prediction, vote_sd, turnout_prediction, turnout_sd, n = 100, conf_level = 0.95)

}
